import {MvpDemoPage} from "../ui/mvp-demo-page";
export default function SettingsPage(){return <MvpDemoPage kicker="ORGANIZATION CONTROL PLANE" title="Organization Settings" summary="Review tenant, chain, contract, provider and treasury registry boundaries. Mutations require an authenticated ADMIN and explicit governed versioning." mode="READ ONLY UNTIL SIWE" flow={["SIWE identity verified","Organization selected","ADMIN role checked","Configuration prepared","Wallet re-confirmed","New version activated"]} panels={[{title:"Organization Boundary",status:"RLS ACTIVE",body:"The server derives organization_id from the selected membership; object IDs never determine tenancy.",facts:["HttpOnly session cookie","CSRF token kept in memory","Cross-tenant reads return no data"]},{title:"Creditcoin Network",status:"CHAIN 102031",body:"All contract identities and Explorer projection are versioned per organization.",facts:["Creditcoin Testnet","HTTPS Explorer allowlist","Confirmed-block readback"]},{title:"Treasury Registry",status:"VERSIONED",body:"Multiple treasuries can run advisory work concurrently while execution preparation is serialized per treasury.",facts:["Treasury/Guard/Registry binding","Rotation cancels stale queued work","Retired versions cannot reactivate"]},{title:"Provider Boundary",status:"NO SECRETS DISPLAYED",body:"Adapters expose configuration metadata only; credentials remain outside the workspace and response payloads.",facts:["Mock remains deterministic default","Agnes narrative is advisory only","No signer or asset authority"]}]}/>}
