const{readFileSync}=require("node:fs");const{resolve}=require("node:path");const{verifyPolicyRegistryArtifact}=require("../dist/contract-surface-engine");const path=resolve(process.env.POLICY_REGISTRY_ARTIFACT_PATH||resolve(__dirname,"../../../contracts/out/PolicyRegistry.sol/PolicyRegistry.json"));const result=verifyPolicyRegistryArtifact(JSON.parse(readFileSync(path,"utf8")));console.log(JSON.stringify(result));if(result.status!=="VERIFIED")process.exitCode=1;
