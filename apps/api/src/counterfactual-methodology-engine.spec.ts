import{validateCounterfactualMethodology}from"./counterfactual-methodology-engine";const valid=()=>({baselineModel:"HOLD_CONSTANT_UNITS_MARK_TO_MARK_V1",observationHorizonSeconds:86400,externalFactorPredicates:["market.benchmark_return.bps","market.volatility.bps"],benchmarkPredicate:"market.benchmark_return.bps",opportunityCostMethod:"BENCHMARK_RETURN_DIFFERENCE_V1",riskAdjustmentMethod:"EXCESS_RETURN_PER_MAX_DRAWDOWN_V1",transactionCostTreatment:"OBSERVED_DISJOINT_COST_REQUIRED",missingDataPolicy:"REFUSE_ASSESSMENT"});describe("counterfactual methodology preregistration",()=>{it("freezes a deterministic no-hindsight methodology without producing a result",()=>{const result=validateCounterfactualMethodology(valid());expect(result.preRegistrationRequired).toBe(true);expect(result.retrospectiveEnrollmentAllowed).toBe(false);expect(result.modelMaySelectBaseline).toBe(false);expect(result.counterfactualResultAvailable).toBe(false);expect(result.netBenefitCalculated).toBe(false);expect(result.assetExecutionAuthorized).toBe(false)});it("rejects cherry-pickable baselines, missing factors and permissive missing-data handling",()=>{expect(()=>validateCounterfactualMethodology({...valid(),baselineModel:"BEST_OF_MARKET"})).toThrow("COUNTERFACTUAL_BASELINE_UNSUPPORTED");expect(()=>validateCounterfactualMethodology({...valid(),externalFactorPredicates:[]})).toThrow("COUNTERFACTUAL_FACTORS_INVALID");expect(()=>validateCounterfactualMethodology({...valid(),missingDataPolicy:"IMPUTE"})).toThrow("COUNTERFACTUAL_MISSING_DATA_MUST_REFUSE")});it("requires observed disjoint costs and a bounded predeclared horizon",()=>{expect(()=>validateCounterfactualMethodology({...valid(),transactionCostTreatment:"IGNORE_COSTS"})).toThrow("COUNTERFACTUAL_COST_TREATMENT_UNSAFE");expect(()=>validateCounterfactualMethodology({...valid(),observationHorizonSeconds:3599})).toThrow("COUNTERFACTUAL_HORIZON_INVALID")})});
