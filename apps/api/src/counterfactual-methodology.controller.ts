import{Body,Controller,Get,Param,Post,UseGuards}from"@nestjs/common";import{AuthContext}from"./auth.service";import{CounterfactualMethodologyService}from"./counterfactual-methodology.service";import{CounterfactualMethodologyTransitionDto,CreateCounterfactualMethodologyDto}from"./counterfactual-methodology.dto";import{IdempotentCommand}from"./idempotency.interceptor";import{activeOrganizationId,CurrentAuth,ORGANIZATION_ROLES,RequireRoles,SessionGuard}from"./session.guard";
@Controller("counterfactual-methodologies")@UseGuards(SessionGuard)@RequireRoles(...ORGANIZATION_ROLES)export class CounterfactualMethodologyController{constructor(private readonly service:CounterfactualMethodologyService){}@Get()list(@CurrentAuth()auth:AuthContext){return this.service.list(activeOrganizationId(auth))}@Get(":id")get(@CurrentAuth()auth:AuthContext,@Param("id")id:string){return this.service.get(activeOrganizationId(auth),id)}@Post()@RequireRoles("ADMIN","TREASURY_COMMITTEE","REVIEWER")@IdempotentCommand()create(@CurrentAuth()auth:AuthContext,@Body()body:CreateCounterfactualMethodologyDto){return this.service.create(activeOrganizationId(auth),auth.userId,body)}@Post(":id/approve")@RequireRoles("ADMIN","TREASURY_COMMITTEE","REVIEWER")@IdempotentCommand()approve(@CurrentAuth()auth:AuthContext,@Param("id")id:string,@Body()body:CounterfactualMethodologyTransitionDto){return this.service.approve(activeOrganizationId(auth),id,auth.userId,body.rationale)}@Post(":id/retire")@RequireRoles("ADMIN")@IdempotentCommand()retire(@CurrentAuth()auth:AuthContext,@Param("id")id:string,@Body()body:CounterfactualMethodologyTransitionDto){return this.service.retire(activeOrganizationId(auth),id,auth.userId,body.rationale)}}
