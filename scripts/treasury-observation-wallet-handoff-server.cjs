const {createServer}=require("node:http");
const {existsSync,mkdirSync,readFileSync,writeFileSync}=require("node:fs");
const {dirname,resolve}=require("node:path");
const HOST="127.0.0.1",PORT=Number(process.env.AEOS_WALLET_HANDOFF_PORT||4178);
const HANDOFF_PATH=resolve(process.env.AEOS_WALLET_HANDOFF_PATH||resolve(__dirname,"../reports/deployment/treasury-observation-wallet-handoff.json"));
const SUBMISSION_PATH=resolve(process.env.AEOS_WALLET_SUBMISSION_PATH||resolve(__dirname,"../reports/deployment/treasury-observation-wallet-submission.json"));
function readHandoff(){const value=JSON.parse(readFileSync(HANDOFF_PATH,"utf8"));if(value.schemaVersion!=="aeos-treasury-observation.wallet-handoff.v1")throw new Error("Unsupported wallet handoff schema");if(value.confirmation?.signed||value.confirmation?.submitted)throw new Error("Wallet handoff must be unsigned and unsubmitted");const tx=value.request?.transaction;if(!tx?.to||tx.value!=="0"||tx.from.toLowerCase()!==value.sender.toLowerCase())throw new Error("Wallet handoff transaction invalid");return value}
function send(response,status,body,type="text/plain; charset=utf-8"){response.writeHead(status,{"Cache-Control":"no-store","Content-Security-Policy":"default-src 'self'; connect-src 'self'; img-src 'self' data:; style-src 'self'; script-src 'self'; frame-ancestors 'none'","Content-Type":type,"Referrer-Policy":"no-referrer","X-Content-Type-Options":"nosniff"});response.end(body)}
async function readBody(request){const chunks=[];let size=0;for await(const chunk of request){size+=chunk.length;if(size>16384)throw new Error("Request body too large");chunks.push(chunk)}return Buffer.concat(chunks).toString("utf8")}
function recordSubmission(payload,handoff){if(!/^0x[0-9a-fA-F]{64}$/.test(payload.transactionHash||""))throw new Error("Invalid transaction hash");if((payload.from||"").toLowerCase()!==handoff.sender.toLowerCase())throw new Error("Submission wallet mismatch");const record={schemaVersion:"aeos-treasury-observation.wallet-submission.v1",recordedAt:new Date().toISOString(),chainId:handoff.chain.chainId,from:handoff.sender,to:handoff.request.transaction.to,transactionHash:payload.transactionHash,requestHash:handoff.request.requestHash,observationId:handoff.request.observationId,evidencePayloadHash:handoff.request.evidencePayloadHash,dataHash:handoff.request.transaction.dataHash,walletConfirmed:true,privateKeyReceived:false,assetExecutionAuthorized:false};mkdirSync(dirname(SUBMISSION_PATH),{recursive:true});if(existsSync(SUBMISSION_PATH)){const existing=JSON.parse(readFileSync(SUBMISSION_PATH,"utf8"));if(existing.transactionHash!==record.transactionHash)throw new Error("Immutable submission record already exists with a different transaction hash");return existing}writeFileSync(SUBMISSION_PATH,`${JSON.stringify(record,null,2)}\n`,{encoding:"utf8",flag:"wx",mode:0o600});return record}
function createServerInstance(){return createServer(async(request,response)=>{try{const handoff=readHandoff();if(request.method==="GET"&&request.url==="/")return send(response,200,readFileSync(resolve(__dirname,"treasury-observation-wallet-handoff.html"),"utf8"),"text/html; charset=utf-8");if(request.method==="GET"&&request.url==="/app.js")return send(response,200,readFileSync(resolve(__dirname,"treasury-observation-wallet-handoff.js"),"utf8"),"text/javascript; charset=utf-8");if(request.method==="GET"&&request.url==="/styles.css")return send(response,200,readFileSync(resolve(__dirname,"evidence-anchor-wallet-handoff.css"),"utf8"),"text/css; charset=utf-8");if(request.method==="GET"&&request.url==="/handoff")return send(response,200,JSON.stringify(handoff),"application/json; charset=utf-8");if(request.method==="POST"&&request.url==="/submission")return send(response,201,JSON.stringify(recordSubmission(JSON.parse(await readBody(request)),handoff)),"application/json; charset=utf-8");return send(response,404,"Not found")}catch(error){return send(response,400,JSON.stringify({error:error.message}),"application/json; charset=utf-8")}})}
if(require.main===module){readHandoff();createServerInstance().listen(PORT,HOST,()=>console.log(`AEOS observation wallet handoff: http://${HOST}:${PORT}`))}
module.exports={createServerInstance,readHandoff,recordSubmission};
