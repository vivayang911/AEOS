const { createServer }=require("node:http"),{existsSync,mkdirSync,readFileSync,writeFileSync}=require("node:fs"),{dirname,resolve}=require("node:path");
const HOST="127.0.0.1",PORT=Number(process.env.AEOS_GOVERNANCE_HOLD_EXECUTE_PORT||4195),HANDOFF_PATH=resolve(process.env.AEOS_GOVERNANCE_HOLD_EXECUTE_PATH||resolve(__dirname,"../reports/live-demo/p0-1-governance-hold-attempt-3-execute.json")),SUBMISSION_PATH=resolve(process.env.AEOS_GOVERNANCE_HOLD_EXECUTE_SUBMISSION_PATH||resolve(__dirname,"../reports/live-demo/p0-1-governance-hold-attempt-3-execute-submission.json")),FROM="0x444d510728fb8072351cb5d0e88432e6a8501dfa",GOVERNOR="0xfe90b087fae789e043514b6ac3dbd7fd2d970268",GUARD="0x3c0cb960f32e6a222149a664a552ffc23e92c628";
function readHandoff(){const value=JSON.parse(readFileSync(HANDOFF_PATH,"utf8")),tx=value.unsignedTransaction;if(value.schemaVersion!=="aeos.live-governance-hold-execute.v1"||value.status!=="HOLD_EXECUTE_REQUEST_PREPARED")throw new Error("Unsupported HOLD Execute handoff");if(value.lineage?.attemptNumber!==3||!/^0x[0-9a-f]{64}$/i.test(value.lineage?.queueArtifactHash||"")||!/^0x[0-9a-f]{64}$/i.test(value.lineage?.queueTransactionHash||"")||!/^0x[0-9a-f]{64}$/i.test(value.lineage?.proposalArtifactHash||"")||!/^0x[0-9a-f]{64}$/i.test(value.lineage?.timelockOperationId||"")||!/^[1-9][0-9]*$/.test(value.lineage?.proposalId||""))throw new Error("HOLD Execute lineage invalid");if(value.proposal?.action?.function!=="setPaused(bool)"||value.proposal.action.paused!==true||value.proposal.action.target?.toLowerCase()!==GUARD||value.proposal.action.value!=="0")throw new Error("HOLD Execute action invalid");if(value.controls?.requiresUserWalletConfirmation!==true||value.controls?.signed||value.controls?.submitted||value.controls?.privateKeyReceived||value.controls?.signerCustody||value.controls?.broadcastCapability||value.controls?.treasuryAssetMovement!==false||value.controls?.assetExecutionAuthorized!==false)throw new Error("HOLD Execute authority boundary invalid");if(tx?.chainId!==102031||tx?.from?.toLowerCase()!==FROM||tx?.to?.toLowerCase()!==GOVERNOR||tx?.value!=="0x0"||!/^0x[0-9a-f]+$/i.test(tx?.data||"")||!/^0x[0-9a-f]{64}$/i.test(tx?.dataHash||""))throw new Error("HOLD Execute transaction invalid");return value}
function recordSubmission(payload,handoff){if(!/^0x[0-9a-f]{64}$/i.test(payload.transactionHash||""))throw new Error("Invalid transaction hash");if((payload.from||"").toLowerCase()!==handoff.unsignedTransaction.from.toLowerCase())throw new Error("Submission wallet mismatch");const record={schemaVersion:"aeos.live-governance-hold-execute-submission.v1",status:"HOLD_EXECUTE_WALLET_SUBMITTED",recordedAt:new Date().toISOString(),chainId:102031,from:handoff.unsignedTransaction.from,to:handoff.unsignedTransaction.to,value:"0x0",transactionHash:payload.transactionHash.toLowerCase(),calldataHash:handoff.unsignedTransaction.dataHash,executeArtifactHash:handoff.artifactHash,queueArtifactHash:handoff.lineage.queueArtifactHash,queueTransactionHash:handoff.lineage.queueTransactionHash,proposalArtifactHash:handoff.lineage.proposalArtifactHash,proposalId:handoff.lineage.proposalId,attemptNumber:3,timelockOperationId:handoff.lineage.timelockOperationId,walletConfirmed:true,receiptVerified:false,proposalExecutedEventVerified:false,timelockCallExecutedEventVerified:false,guardStateVerified:false,outcomeEvidenceCreated:false,privateKeyReceived:false,signerCustody:false,broadcastCapability:false,assetExecutionAuthorized:false};mkdirSync(dirname(SUBMISSION_PATH),{recursive:true});if(existsSync(SUBMISSION_PATH)){const existing=JSON.parse(readFileSync(SUBMISSION_PATH,"utf8"));if(existing.transactionHash!==record.transactionHash||existing.executeArtifactHash!==record.executeArtifactHash)throw new Error("Immutable HOLD Execute submission already exists with different identity");return existing}writeFileSync(SUBMISSION_PATH,`${JSON.stringify(record,null,2)}\n`,{flag:"wx",mode:0o600});return record}
function send(res,status,body,type="text/plain; charset=utf-8"){res.writeHead(status,{"Cache-Control":"no-store","Content-Security-Policy":"default-src 'self'; connect-src 'self'; style-src 'self'; script-src 'self'; frame-ancestors 'none'","Content-Type":type,"Referrer-Policy":"no-referrer","X-Content-Type-Options":"nosniff"});res.end(body)}async function body(req){const chunks=[];let size=0;for await(const chunk of req){size+=chunk.length;if(size>16384)throw new Error("Request body too large");chunks.push(chunk)}return Buffer.concat(chunks).toString("utf8")}
function createServerInstance(){return createServer(async(req,res)=>{try{const handoff=readHandoff();if(req.method==="GET"&&req.url==="/")return send(res,200,readFileSync(resolve(__dirname,"live-governance-hold-execute-wallet-handoff.html"),"utf8"),"text/html; charset=utf-8");if(req.method==="GET"&&req.url==="/app.js")return send(res,200,readFileSync(resolve(__dirname,"live-governance-hold-execute-wallet-handoff.js"),"utf8"),"text/javascript; charset=utf-8");if(req.method==="GET"&&req.url==="/styles.css")return send(res,200,readFileSync(resolve(__dirname,"evidence-anchor-wallet-handoff.css"),"utf8"),"text/css; charset=utf-8");if(req.method==="GET"&&req.url==="/health")return send(res,200,JSON.stringify({status:"PASS",artifactHash:handoff.artifactHash,proposalId:handoff.lineage.proposalId,chainId:102031,value:"0x0",assetExecutionAuthorized:false}),"application/json; charset=utf-8");if(req.method==="GET"&&req.url==="/handoff")return send(res,200,JSON.stringify({handoff,submission:existsSync(SUBMISSION_PATH)?JSON.parse(readFileSync(SUBMISSION_PATH,"utf8")):null}),"application/json; charset=utf-8");if(req.method==="POST"&&req.url==="/submission")return send(res,201,JSON.stringify(recordSubmission(JSON.parse(await body(req)),handoff)),"application/json; charset=utf-8");return send(res,404,"Not found")}catch(error){return send(res,400,JSON.stringify({error:error.message}),"application/json; charset=utf-8")}})}
if(require.main===module){readHandoff();createServerInstance().listen(PORT,HOST,()=>console.log(`AEOS Decision-bound HOLD Execute handoff: http://${HOST}:${PORT}`))}module.exports={createServerInstance,readHandoff,recordSubmission};
